{
  "name": "apple-llm-performance-checks",
  "private": true,
  "description": "Browser checks for the built page. The site itself has no build step and no runtime dependencies; this is tooling only.",
  "scripts": {
    "smoke": "node tools/smoke.js docs/index.html"
  },
  "devDependencies": {
    "playwright": "1.49.1"
  }
}
